= content_for(:main_content_header) do
  .row
    .col-sm-12
      .page-header
        %h1= t('.header')

= render 'progressbar', active: 4, progress: (100 / 5) * 4
= render partial: 'rooms/form'

.hidden
  = form_tag [:wizard, :step4_post], method: :post, class: 'step4-finish-form', remote: true do
    = submit_tag 'submit'
