$('#line-example').empty();
Morris.Line({
    element: 'line-example',
    data: <%= raw @data.data.to_json %>,
    xkey: '<%= @data.xkey %>',
    parseTime: false,
    ykeys: <%= raw @data.ykeys.to_s %>,
    labels: <%= raw @data.labels.to_s %>,
    postUnits: '€',
    smooth: false
});