en:
  activerecord:
    attributes:
      channel_manager/previo:
        lcode: Hotel ID in Previo Admin panel
      company:
        name: Company name
        reg_number: Registration number
        reg_address: Registration address
        logo: Logotype
      room:
        min_price: Minimum price
        max_price: Maximum price
      room_setting:
        position: Desired position among competitors
      setting:
        districts: City districts
      user:
        invitation_accepted_at: Join Date
    errors:
      models:
        room:
          attributes:
            min_price:
              greater_than_or_equal_to: "must be greater than or equal to <em>&euro; %{count}</em>"
            max_price:
              greater_than_or_equal_to: "must be greater than or equal to <em>&euro; %{count}</em>"
