= content_for(:main_content_header) do
  .row
    .col-sm-12
      .page-header
        %h1= t('.header')

= render 'progressbar', active: 5, progress: (100 / 5) * 5

= render template: 'related_hotels/edit'

- if @related.size > 0
  = link_to [:wizard, :complete], class: 'btn btn-lg btn-primary' do
    = icon('flag-checkered')
    = t('.finish')
